
describe('Utilities for running fractal inside workers', () => {

  it('should run fractal over a worker', () => {})

  it('should react to inputs', () => {})

  it('should dispatch tasks', () => {})

})
